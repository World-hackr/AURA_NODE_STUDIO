# Phone UI Tests

This folder is reserved for future phone-side tests and lightweight validation projects.

When the phone UI exists, keep its tests here instead of mixing them into `host_remote/tests/`.
